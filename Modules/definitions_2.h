#ifndef DEF_2_H
#define DEF_2_H

#include <stdio.h>
#include <stdlib.h>

typedef struct StructProfile {

char * username;
char * status;
char * friends;
struct StructProfile * RequestHead;
}Profile;

#endif
